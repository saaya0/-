<h2>My page</h2>

<%= form_with model:@user, url:user_path(current_user), local:true do |f| %>
<h4>
  name
  <%= f.submit '保存' %>
</h4>
<%= f.text_field :name %>
<% end %>

<h4>
  e-mail <br>
  <%=@user.email %>
</h4>
<% end %>
<%= link_to "favorite", favorite_path, class: "btn btn-secondary" %>
<%= link_to "退会", confirm_unsubscribe_path(@user), class: "btn btn-secondary" %>

<h4>お気に入り</h4>
<% if @favotite_list.present? %>
  <% if @favorite_list.each do |spot| %>
    <%= spot.name %>
    <%= image_tag spot.spot_img %>
  <% end %>
<% else %>
お気に入りをしておりません。
観光地の詳細ページからお気に入りが出来ます。
<% end %>
